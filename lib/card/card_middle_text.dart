import 'package:flutter/material.dart';

class CardMidText extends StatelessWidget {
  const CardMidText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      "This official website features a ribbed knit zipper jacket that is modern and stylish."
      " it looks very temprament and is recomment to friends",
      style:
          TextStyle(fontSize: 14, fontFamily: 'Montserrat', color: Colors.grey),
    );
  }
}
